# TASK-028 — Local AWS preparation justification

Owner-approved release sequence, specifically “Prepare locally; no AWS spending yet”. REQ-FOUND-012 AC-01–04. No apply, provisioning, purchases, DNS changes or messages authorized.

Prepare additive CloudFormation foundation and edge-access templates plus offline security checks. Separate private database, immutable registry, encrypted storage/keys, task identities, logs and cost-alert configuration. Templates are reviewable preparation, not a running staging environment. Preserve the approved topology history; managed private endpoint egress is a provisional alternative requiring a revised calculator estimate before apply. Public routing/service activation remains gated by a separately verified staging runtime, account/domain/budget and tester inputs.

Tests KEEP all application regressions; ADD offline template schema validation and policy checks for private data, retained resources, exact security-group ingress, key permissions, deny-by-default edge access and no default budget/account. No existing infrastructure edited/deleted. No deploy/teardown script is added. Document unverified provider behavior and remaining resources rather than mark deployment acceptance complete.

## Staging integration continuation — owner “ok continue”, 2026-09-20

Resume after the approved master merge on task/TASK-028-staging-integration from b2cfacf; prior task branch/history retained. Add a separate synthetic staging entry point, explicit TLS database configuration, server origin-secret guard, cookie-only auth, private durable synthetic delivery and least-privilege database bootstrap/migration tooling. Add a complementary CloudFormation application stack (ALB/CloudFront/ECS/EFS/TLS/alarms) using foundation outputs; retain original templates and local entry points unchanged. No AWS execution, role revocation, schema deletion or production flag.

KEEP all tests and foundation policy obligations. ADD staging configuration/guard denials, real PostgreSQL role/audit/write/no-DDL checks, delivery persistence, application-stack cache/permission/activation tests and staging artifact verification. Application stack starts with zero desired tasks and requires explicit later activation/parameter evidence. Account/domain/cost/real KMS/CloudTrail/ISP checks remain unrun, not silently accepted. Current image scan evidence will be refreshed after implementation. No change to clinical/product requirements.

Protected sink follow-up: add a separately invocable read-only EFS task that seals one UUID-selected synthetic message to an operator RSA public key, and a local private-key decryption command that writes an ignored 0600 file with exclusive creation. No plaintext token cloud logs, network mail, automatic invocation or database credentials in this job. ADD round-trip/wrong-key/path-traversal tests; KEEP original local mailbox command. Named operator IAM approval and real EFS/CloudTrail verification remain deployment gates. API/web/migration image rebuilds include additive runtime files; serving entry points and application behavior retained.

Final scope clarification: foundation receives additive outputs, an optional exact-distribution S3 read grant and CloudWatch-to-SNS/storage-alarm wiring; its original private subnet/no-service controls and insecure-transport deny are preserved, not replaced. New application stack adds generic runtime failure alarms. Ignore only validator-generated Python bytecode; retain the generated files locally. Full reports remain available, including migration-image vulnerabilities; no scanner exclusions or test weakening. Release runbook explicitly separates local checks from missing operator/cost/deployed evidence. Existing compose.yaml and read-local-mail.ts edits remain excluded with their original diff hash.

## Next approved release step — owner merge/continue instruction, 2026-09-20

Remote master now verified at 3cf00e7 after all-branch ancestry integration. Reuse TASK-028 branch at that base. FIND-013's current migration-image high/critical occurrences are in original OS/global npm/pnpm packages; do not waive them. Add a distroless migration candidate using the same pinned Node/Prisma/schema/history, an explicit direct-Prisma invocation mode, required OpenSSL libraries with package metadata and the existing bootstrap/migrate commands. Preserve the original migration image, pnpm path, package graph and tests. No dependency/file removal or legacy behavior change authorized.

KEEP all current checks. ADD real TLS PostgreSQL isolated-image bootstrap, migrate twice/idempotency, restricted runtime writes/history denials and original migration checksums; inspect non-root/no-shell/installer invariants and scan complete candidate inventory. Candidate must pass before recommendation. No remote jobs, AWS spending, automatic migrations or push of subsequent work authorized by the completed merge operation. Record actual scanner findings, including failures, without suppression. Hosted CI status is currently unreadable through unauthenticated gh; SSH push/ref verification succeeded.

2026-09-21 continuation: focused candidate copies only the installed Prisma dependency closure (including its assets/native engine) plus a bundled operator command; original workspace/package graph/image retained. Initial candidate scan has no high/critical findings. Real image execution exposed incompatible native Prisma TLS URL syntax in the unexecuted staging job: require + sslaccept=strict + explicit CA is the native connector equivalent of verified TLS. Preserve pg's rejectUnauthorized=true; ADD native-connector wrong-CA/hostname tests rather than treating driver-only tests as migration proof. No raw secret-bearing diagnostics enabled. Prisma update/telemetry checks disabled for operator jobs.

Final implementation preserves UID1000 in existing job definitions by appending an account entry to the candidate's copied base passwd file (all base entries retained). Prisma's native loader requires an OS user/home lookup even with update checks disabled; no host HOME override, shell or package installer is added. Native TLS URL construction is shared with the real-engine denial test. The frozen closure copier resolves installed package manifests, copies declared/installed optional dependencies and retains package assets; this supports the bounded migrate deploy/status commands, not every Prisma development command. All original dependencies, migrations, stages and tests remain. New image test asserts one real runtime write, original migration checksums/idempotency, special-character credentials, verified CA/hostname denials, history/DDL/deletion denials and no shell/npm/pnpm/perl.
