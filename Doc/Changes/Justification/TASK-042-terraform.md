# TASK-042 — Change justification

Owner explicitly approved Terraform alongside CloudFormation. REQ-FOUND-012 local preparation; no AWS spending. Add a native Terraform alternative with equivalent staging security/isolation/retention and operator boundaries, independent resource ownership and mocked tests. Preserve original CloudFormation/Python/cfn-lint files and prior decisions. Do not allow both tools to own the same resources. Local state/plan files ignored; future remote encrypted/locked state and account approval required before apply.

KEEP every Python policy assertion and existing application test. ADD TypeScript equivalents of all ten policy scenarios, malformed-template rejection, Terraform fmt/schema validation and mocked plan/negative-input checks. TypeScript policy checks are not a reimplementation of the entire cfn-lint provider schema engine; Terraform validates its own provider schema. Preserve optional cfn-lint instructions. No Python introduced in new tooling. No resource/import/apply/destroy or secret fetch is run. Document exact verification and any limitations, source parity and production gates before completion.

Final areas: native network/storage/IAM/edge/runtime/monitoring/outputs/variables and provider lockfile; a separate credential-free CI job; nine mock-provider plan runs. All ten Python scenarios are ported assertion-for-assertion plus malformed/unknown-tag parser rejection. No CloudFormation or Python source was edited. Missing provider-computed values in mock plans use explicit synthetic mocks; omitted subscriber emails are asserted as empty/null, not ignored. Private route lists are explicitly empty. No assertion is removed to hide failures. Local provider sockets required sandbox escalation; no AWS credentials or clients used.
