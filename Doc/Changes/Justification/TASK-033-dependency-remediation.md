# TASK-033 — Dependency remediation rationale

Owner approval 2026-09-14: implement TASK-033 and recommended local merge, push/CI verification and package rebuild. TASK-034 merged locally; owner separately confirmed push to origin/master after automatic review requested explicit remote authorization. Master c7c5a38 pushed successfully; GitHub CLI lacks authentication so hosted result is unverified.

REQ-FOUND-012 / FIND-013: five npm advisories and missing OS scan coverage require dependency-path/reachability analysis and minimal compatible remediation. KEEP all existing tests, behavior and direct workspace dependencies. Inspect vendor patches before choosing upgrades. ADD regression coverage for patched behavior when relevant; run full current verification, database/browser/package checks and local image scanner. Preserve original SBOM/evidence; new evidence gets TASK-033 paths. No removals of source/tests/APIs/records, cloud deployment or real data. Runtime minimization assessed, not silently applied. No broad major stack upgrade.

Initial evidence: image-size 1.2.1 comes through native Metro tooling and has no published patched version; deepmerge-ts 7.1.5 comes through Prisma config, whose v8 fix changes semantics; mysql2 3.15.3 comes through Prisma CLI with compatible 3.x fixes. Assess exact callers before updating. Scanner will inspect a Docker export locally without giving it Docker socket or credentials. Synthetic state and original images retained for rollback.

Chosen remediation: scoped pnpm overrides for @prisma/config>deepmerge-ts 8.0.2 and prisma>mysql2 3.23.1. Prisma loads only repository-owned plain config, remote/rc/package config extension disabled; no Maps/recursive records are used. Verify actual config loading and circular-reference handling plus migration/build workflows to guard the v8 semantic change. No Prisma/Node/React major upgrade. Image-size has no patched registry release; retain it and document Metro-only reachability and public release gate rather than inventing a vendor patch or hiding the advisory.

OS remediation: scan found fixed Debian libpcre2 advisories. Install pinned libpcre2-8-0 10.42-1+deb12u1 and openssl 3.0.20-1~deb12u2 in existing packaged Dockerfile; the latter also fixes Prisma's missing OpenSSL autodetection warning. Preserve Node/pnpm versions and existing image capabilities. Unfixed Debian and bundled package-manager findings will stay explicit release blockers with complete scan evidence.

Review note: generated lockfile changes include upstream mysql2 transitive dependency replacement (seq-queue/sqlstring replaced by sql-escaper) and peer-key updates; no direct workspace dependency, application API or repository source file is removed. This is the upstream patched 3.x package dependency closure, verified by frozen install and regressions.
