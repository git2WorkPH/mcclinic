# TASK-025 onboarding justification
Owner approved onboarding and database-name mcclinic on 2026-09-12. REQ-FOUND-011 AC-01–05, ADR-007.
Problem: SaaS currently requires seeded accounts and cannot verify/invite/recover users or enforce MFA. Config still names ehr_mvp/ehr_dev despite owner naming instruction.
Change: additive identity/challenge/MFA records, application use cases and isolated persistence/delivery/crypto ports; actual forms, generated GraphQL contract, private local delivery capture, non-destructive database rename command and canonical mcclinic configuration. Legacy login remains supported without MFA until enrolled. No clinical/practice user-edited files are overwritten.
Test classifications: KEEP all existing assertions. ADD verification/reset/invite/token expiry/single-use/race/rate/session revocation/MFA replay/tenant-seat cases and browser onboarding. UPDATE only migration harness setup through existing all-migration mechanism. Test database rename on isolated container; never DROP/reset/overwrite databases. No test or supported behavior deletion.
User changes present at start: MVP_RUNBOOK; patient domain/use cases; certificate validation; practice scope/services; PracticeSettings; new Prisma skills/.claude/.windsurf/skills-lock. Preserve and exclude unrelated edits from commit. Read actual diffs before overlapping changes; prefer new onboarding files and additive integration.
Verify lint/boundaries/typecheck/codegen/build, unit/API, PostgreSQL and Playwright; review final diff and update memory/acceptance. Production email/MFA recovery/payment policies stay Open, not legal requirements.

Review refinements: credential-version-bound reset tokens and a session-open version check prevent older reset links/in-flight login from surviving credential rotation. Transactional factor consumption records an audit event; browser hash-change handling supports links opened in an existing tab. ADD tests preserve the failure-producing scenarios; no existing tests/assertions were removed, skipped or weakened. The seed now grants default-practice memberships only to legacy email-null accounts: every previously supported seed account remains supported, while newly registered practice accounts cannot gain unintended default-practice access. No previous migration is modified; the new TASK-025 migration was refined only before any persistent deployment (isolated test containers only).
