# Implementation change justifications
Canonical location required by the owner: `Doc/Changes/Justification/<TASK-ID>-<description>.md`.
Before any future implementation edit, create this record from `Documentation/Templates/CHANGE-JUSTIFICATION-TEMPLATE.md` after verifying explicit task approval, relevant requirement criteria and accepted ADRs. Cover code, tests, configuration, dependencies, migrations and follow-up fixes. Classify tests KEEP/ADD/UPDATE/SPLIT/REMOVE by requirement. Record exact removals and separate explicit deletion approval; failing tests never authorize deletion, weakening or skipping. No deletions are currently approved.
Proposal bookkeeping does not require an implementation justification. No speculative implementation records have been created.
