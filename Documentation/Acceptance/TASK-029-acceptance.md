# TASK-029 — Local identity preparation evidence

Reviewed against base 26bdb0b on 2026-09-20. Owner-approved local release sequence; no AWS spending/activation. Status: local preparation verified; full task remains Approved pending deployed/provider evidence. ADR-013 and canonical task justification govern the change. Original migrations, APIs, local modes and test assertions retained; no removals.

| Criterion               | Local implementation and evidence                                                                                                                                                                                                                              | Remaining gate                                                                   |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| Threat/recovery policy  | IDENTITY_READINESS: token leakage, replay, mailbox compromise, device/key loss and support escalation. No operator MFA bypass or clinical entitlement                                                                                                          | Accountable identity-proof, privacy and operator policy approval                 |
| Transactional delivery  | Additive IdentityOutbox, encrypted payload, account/token/audit atomic enqueue, leased competing workers, retry/exhaustion and stable-ID sink. Outbox integration tests prove rollback, concurrent claims, acknowledgement-audit retry and retained exhaustion | Real provider/domain verification, delivery/bounce authentication and monitoring |
| Versioned custody       | AES-GCM key-ID binding; retained previous versions and legacy decrypt compatibility. Real enrolled MFA test verifies rotation and separately restored key file. KMS adapter validates ARN/context/length; injected-provider tests prove failure/restore        | Actual KMS/IAM/wrapping/CloudTrail and disaster recovery drill                   |
| Session/tenant security | Exact HTTPS Origin+JSON POST; HttpOnly Secure SameSite cookie; nonsecret alias-safe login marker; no bearer fallback; persistent login budgets. Database test covers origin rejection, secret exposure, tenant denial and logout                               | Edge budgets, deployed TLS/proxy and independent security review                 |
| Browser compatibility   | Built app tested through local HTTPS proxy: login, reload, logout; secret absent from JavaScript storage/cookies. Seven original clinical/onboarding/print journeys pass unchanged                                                                             | Real ISP/staging performance and security evidence                               |

Commands: `pnpm check` PASS (30 tests, Oxlint/boundaries/types, Prisma generation and API/web builds). Five integration files PASS (30 scenarios): outbox, onboarding, MVP, database, identity-browser. `pnpm test:mvp:web` PASS (7). Codegen PASS with no generated diff; changed TypeScript formatting and Git whitespace pass. Node 24.21.0, pnpm 10.34.5; Docker PostgreSQL17.6 and local Chromium/OpenSSL. Complete current logs retained in TASK-029-artifacts.

Earlier interrupted run: local sandbox denied test listeners; rerun with socket permission passed. A prior unchanged clinical journey stalled at check-in and consequently lacked print-audit evidence; current full unchanged rerun passed. No test disabled, removed or weakened. New cookie-denial test originally queried nullable `me`; corrected its request to protected `patients` while retaining the denial assertion, consistent with existing API requirements.

Review: persistence remains in infrastructure; domain/application contracts unchanged. Audit acknowledgements and state updates are transactional. Outbox fields never expose cleartext tokens; error logs are generic. Explicit mode selection preserves original local behavior and generated contracts. KMS is never instantiated by local tests except the denied-activation check; no AWS calls. Cookie mode has no production entry-point approval. No new clinical authorization or support powers. Generated cache ignored, not deleted.

Unrelated infrastructure/docker/compose.yaml and tooling/read-local-mail.ts edits preserved/excluded. Existing task035/task033 images are prior snapshots; this source change needs a new serving-image build/scan before any later image rollout. FIND-013 and Philippine clinical/privacy/legal/signature/retention findings remain Open. Local preparation is not production readiness.
