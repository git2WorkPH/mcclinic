# TASK-035 acceptance/review

Owner-approved local sequence; base 0523a7e. ADR-012 accepted for additive serving/migration separation. Original Dockerfile, APIs, tests, dependencies and state preserved. No deletion or clinical behavior change.

API image: sha256:452eff3ea461204af4f33e708cb6e93e5784778c422bde73a2d6aed4575a54b3 (57,377,328 bytes). Web: sha256:7d7a81496e131d914bf5aa57aa8da7da5b646b2c2cbccea01f3ff25d553374a7 (55,323,446 bytes). Full migration/seed image: sha256:30105abbcbf1c175e5ef20d63aae6bdaab42ccb041cc591b90ecbdc08b8fbb96. Node remains 24.21.0; UID1000 preserves state ownership.

Trivy 0.74.0 scans of each serving image: 16 low/14 medium Debian occurrences, no high/critical; bundled Node dependency scans report no findings. Complete reports and bundle SBOMs preserved in TASK-035-artifacts. No ignores/mutes. Source-relative assets retained during bundling, unknown external imports rejected except optional pg-native and builtins. Runtime inspection confirms no shell, npm or workspace node_modules. Full migration image is intentionally not a hardened serving image; its known tooling findings remain in FIND-013. Scan limitations/dates/vendor status retained; recheck by 2026-09-21 or before exposure changes.

Verification: pnpm check passed (28 tests, lint, types, builds); codegen drift passed; all seven unchanged packaged browser journeys passed on separate images. Recovery rehearsal restored all public table counts/hashes, decrypted enrolled MFA secrets using backed-up key without logging them, verified readiness after old/new image rollback and preserved every table hash. First recovery attempt exposed startup readiness race; bounded readiness wait added, no assertion weakened. Test-only containers stopped and evidence/backups retained. All 23 PostgreSQL integration scenarios and both foundation browser tests passed (60 total existing scenarios). Commands: `pnpm exec vitest run tests/onboarding.integration.test.ts tests/mvp.integration.test.ts tests/database.integration.test.ts` and `pnpm test:web`.

Review: dependency direction and clinical contracts unchanged; generated bundle outputs ignored, original package remains. Private data/config guards, non-root state, audit/snapshot/tenant regressions preserved. No public deployment/real data or claimed production approval. New SERVING_RUNBOOK documents operation, rollback and scanner scope. Separate image variants meet the approved local task; remaining identity and deployed recovery requirements are separate TASK-029/030.

Review scope: all TASK-035 changes against 0523a7e, including new Dockerfiles, bundler, recovery verifier and retained reports. No source/test assertions removed. Unrelated `infrastructure/docker/compose.yaml` username edit is preserved and excluded. Decision: completed for local synthetic use; no public risk acceptance.
