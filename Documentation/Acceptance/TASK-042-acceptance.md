# TASK-042 — Native Terraform and TypeScript policy acceptance

2026-09-21. Reviewed new candidate and tooling against 993142aed07026c861f72d9db3de2ab66e7d9807. Owner explicitly selected “Implement Terraform alongside CloudFormation now”. Local preparation scope completed; no AWS provisioning/spending/API calls, real credentials, external messages or resource/state changes.

Native AWS resources under infrastructure/terraform/staging cover the four preserved CloudFormation areas: private network/RDS/stores, exact secret/IAM/job identities, private Fargate/EFS, edge WAF/ALB/CloudFront/origin/caching/print headers/DNS, retained stores and budget/alarm definitions. Terraform1.14.9 CLI archive checked against HashiCorp SHA256; AWS6.65.0 provider reports signed by HashiCorp and is locked. These are tested pins, not a latest-version claim. Both provider aliases mocked; all test runs explicitly plan only. Initial plugin socket restriction passed with local process access; no AWS credentials used.

| Acceptance                           | Result                                                                                                                                                                                                                                                                             |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Terraform formatting/provider schema | terraform fmt -check -recursive and validate PASS; init -backend=false installs tooling only.                                                                                                                                                                                      |
| Security/negative mocked plans       | Nine runs PASS: positive private/inactive/encrypted/no-cache/permissions/retention/WAF/budget plan; reject unapproved provisioning, public tester CIDR, activation without custody, mutable image, shared database secret, missing budget, wrong AZ region and weak origin secret. |
| Python assertion parity              | tests/staging-policy.test.ts retains all ten scenarios/assertions from test_staging_policy.py and test_staging_application.py; adds unknown-tag/duplicate-key/missing-resource rejection. Eleven tests PASS, included in pnpm check/CI. No Python interpreter required for them.   |
| Preservation/ownership/state         | No existing CloudFormation/Python/validator source changed or deleted. Native Terraform resource ownership and state-bearing origin-secret differences mapped in README/ADR-017. .tfstate/.tfplan/.tfvars/provider dirs ignored; all original files/history retained.              |
| CI and regressions                   | New read-only workflow runs locked init, fmt, validate and mocked tests without AWS credentials; existing CI steps retained plus compiled dotenv check. Hosted CI not run (no push). Shared pnpm check54/runtime8/browser9 tests PASS; logs linked below.                          |

[Shared exact logs](TASK-041-artifacts/2026-09-21/). Mock-plan computed attributes are explicitly synthetic; no actual cloud success inferred. Initial mock assertions encountered unknown computed IDs/null omitted emails; supplied synthetic ID mocks and explicitly empty private routes, preserving the obligations. Existing assertions never weakened. TypeScript checks are custom policy parity, not the full cfn-lint schema engine; legacy schema validation remains optional for changes to CloudFormation. The native provider validates its own Terraform schema.

Project-review decision: accept bounded local TASK-042. Review verified source-account task trust, separate bootstrap/runtime secrets, encrypted private data, no native automatic jobs/email/purge, printed-document CSP preserved and no clinical cache. Protected state and owner/account/budget/operator approval are mandatory before any real plan/apply. prevent_destroy limitations, singleton ownership, input/image/secret consistency, seven-day PITR/default backup policy, real monitoring/anomaly delivery, IAM/EFS/RDS/KMS/CloudFront/recovery/ISP verification remain explicit gates. Existing FIND-013/production findings are Open. Full TASK-028/029/030/031 are not completed by this task.

Unrelated compose.yaml and read-local-mail.ts edits unchanged and excluded; diff SHA256 c717836a243d8d6c76afcf557cbea8d231473ac67c67b50ab32b4581e76791d6. No resource/file/schema/test deletion. No blanket clinical/legal compliance claim.
