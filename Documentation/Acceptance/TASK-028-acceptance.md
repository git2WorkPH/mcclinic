# TASK-028 — Local foundation checkpoint

2026-09-20, base d6ac8a1. Status: Approved / In progress, not Completed. Owner authorizes local preparation only; no AWS spending.

Delivered two additive CloudFormation templates, pinned offline validator environment, five policy tests and a runbook. ADR-014 records provisional endpoint egress and the revised cost gate. Schema validation passes for Singapore foundation and us-east-1 edge policy; five offline tests pass. No AWS API, resource, message, billing, deployment or DNS change. Validator schema acceptance is not deployed proof.

AC-01/02 local evidence: private encrypted RDS with managed master secret, exact runtime security-group ingress, retained storage/keys/images/logs, runtime role restricted to contextual KMS decrypt, execution role uses a separate runtime secret parameter, tester default-block and explicit cost thresholds. Runtime/execution IAM separation is present; database-role bootstrap/migration identity, active distribution/origin/cache controls, runtime wiring and full operational alarms are still pending. Budget/topic configuration does not prove alert delivery or cap spending.

AC-03/04 deployed evidence: NOT RUN. No account, budget, domain, deployed endpoint or tester approval has been provided; none inferred. No service is running from these templates. Full task remains Approved with remaining work in infrastructure/aws/README.md. Actual RDS version availability, IAM, TLS, runtime bootstrap, cost estimate, alert/anomaly delivery and all staging/ISP/drift/recovery checks remain gates.

Review: no existing infrastructure changed, no deletion/lifecycle purge, no secrets in outputs, no broad runtime public ingress and no raw request sampling. Seven-day proposed PITR retention requires operational acceptance; it is not a clinical retention finding resolution. Original ADR-008/009 and deployment cost/history preserved. Existing application checks need not be repeated for this isolated template checkpoint; schema and negative policy checks cover the change.

## Application integration and review — 2026-09-20

Reviewed complete task continuation against b2cfacf on `task/TASK-028-staging-integration`, including uncommitted additions. Owner “ok continue” / “continue”; local preparation only. Historical foundation checkpoint above remains preserved. ADR-015 governs the additive runtime/job decisions. Canonical task remains `Tasks/Approved/TASK-028.md`.

Implemented separate fail-closed synthetic staging entry point; explicit CA/hostname-verified PostgreSQL TLS; exact-origin cookie-only sessions and origin secret guard before API routes; transactional outbox/private durable synthetic sink; separate database role bootstrap/migration and reviewed grants; ALB/CloudFront/ECS/EFS/task/job/alarm templates; zero caching for API and errors; private S3 OAC; sealed read-only operator mailbox retrieval; release/rollback/drift instructions. All original local entry points, schema/migrations, tests, dependencies and clinical behavior preserved. Existing foundation controls remain; only additive outputs/OAC/monitoring grants are added. No deployment or external mail.

| Criterion                          | Local evidence                                                                                                                                      | Remaining acceptance                                                                                                                      |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| AC-01 plan/apply/least privilege   | Four schema-validated templates; ten policy checks; PostgreSQL role denials and atomic audit rollback; runtime/TLS/origin/cookie guard tests        | Actual change sets/account IAM/RDS/EFS/KMS/CloudFront behavior, secret custody and least-privilege operator approval unverified; no apply |
| AC-02 cost/notifications           | Existing budget thresholds retained; health/5xx/runtime-failure/storage alarm definitions, restricted CloudWatch publisher, no external subscribers | Fresh account-specific calculator ceiling, cost anomaly preparation and actual notification/recovery coverage; unapproved/unrun           |
| AC-03 synthetic deployed workflows | 34 unit/API checks; 33 PostgreSQL/TLS/browser integration scenarios; seven packaged and two foundation browser journeys; no AWS calls               | Actual allowlisted endpoint, membership/tenant/clinical/print smoke and migrated RDS verification not run                                 |
| AC-04 recovery/drift               | Ordered non-destructive release/rollback/restore protocol; retained artifacts and local persistent-restart regression                               | Real drift/recreate/PITR/key/sink restore and target-ISP/load evidence not run; no destructive operation authorized                       |

Verification commands/results, Node24.21.0/pnpm10.34.5, Trivy0.74.0, cfn-lint1.57.0:

- `pnpm check`: PASS, lint/boundaries/types, 34 tests, Prisma generation and API/web builds. Final focused lint/types passed after test/operator-tool additions.
- `pnpm codegen` plus `git diff --exit-code -- packages/graphql-contract/src`: PASS/no drift.
- `pnpm exec vitest run tests/staging-tls.integration.test.ts tests/staging-roles.integration.test.ts tests/outbox.integration.test.ts tests/identity-browser.integration.test.ts tests/onboarding.integration.test.ts tests/mvp.integration.test.ts tests/database.integration.test.ts tests/release-network.integration.test.ts`: final PASS, eight files/33 scenarios.
- `SERVING_IMAGES=true IMAGE_TAG=task028 node tooling/verify-packaged.mjs`: final images PASS, seven browser journeys and persisted restart in a separate Compose project. Test containers stopped, state/volumes retained; existing mcclinic-packaged task033 clinic untouched.
- `pnpm test:web`: PASS, two existing foundation journeys.
- `pnpm exec vitest run tests/staging-mailbox.test.ts`: PASS, including actual local decryption CLI, wrong-key/path denial, no token stdout and exclusive-file preservation.
- Four templates pass offline cfn-lint; foundation/application policy suites pass 5+5. All schema validation is offline, not AWS API execution.
- Five ARM64 image targets build. Final API/web/staging/mailbox each: 16 low/15 medium OS occurrences, zero high/critical. Four bundled dependency SBOM scans: no findings. Migration image: 82 low/103 medium/60 high/4 critical/3 unknown, **not accepted for remote execution**. No filters, ignores or scanner mute. See FIND-013; refresh by 2026-09-21 or before exposure changes.

Exact image digests, full scan reports, SBOM/build metadata, clean final logs and SHA-256 evidence manifest: [integration artifacts](TASK-028-artifacts/integration-2026-09-20/). Images are locally built working-tree candidates labeled b2cfacf-task028-working, not published releases or ECR digests. Rebuild/publish a reviewed immutable revision only after separate authorization. Bundle scans complement OS/image scans; none certify exploitable reachability or clinical/legal safety.

Investigation history: first HTTP test lacked sandbox permission for loopback; it passed with authorized local access. First role migration required database CREATE for the migration role (runtime remains denied). TLS-negative test initially used an SNI value overridden by pg; corrected the connection to preserve intentionally wrong SNI and asserted ERR_TLS_CERT_ALTNAME_INVALID. Final real TLS tests prove chain acceptance plus wrong-CA and wrong-hostname rejection. One scan command misparsed a zsh variable colon; corrected tag quoting and scanned exact final images. No assertion or original test was deleted, skipped or weakened.

Project-review decision: local integration checkpoint accepted; full task remains Approved/In progress. Dependency boundaries, clinical immutability and server authorization preserved. No source/test/file/schema/API removal. OAC avoids duplicate bucket-policy ownership; reader IAM is mount-only and filesystem read-only with ciphertext-only output; original history/audit triggers remain. No credentials, raw patient records or plaintext identity links in tracked evidence. Unrelated compose.yaml and read-local-mail.ts changes excluded, unchanged diff SHA256 c717836a243d8d6c76afcf557cbea8d231473ac67c67b50ab32b4581e76791d6.

Open gates: migration/tooling vulnerabilities (FIND-013), revised cost/anomaly/operator-monitoring preparation, actual RDS bootstrap permissions and all provider/deployed acceptance. Sealed-reader operation needs named operator permission and actual EFS/CloudTrail verification; local encryption tests are not those approvals. Logs-based failure alarms do not prove a silent worker healthy; exhausted/old intents and sink backup checks remain documented operator checks until monitoring is completed. Philippine clinical/privacy/legal/signature/retention findings remain Open; TASK-031 stays NO-GO. No push, merge, AWS spending or real data.

Final staging checks: formatting passed for all 30 supported staged source/document files; source whitespace check passed. Raw retained CLI logs keep their original trailing whitespace/blank EOF bytes and are excluded from source whitespace formatting so their evidence hashes remain valid. All artifact SHA-256 values verified before commit. Session memory records observed pre-commit HEAD/state rather than an impossible self-referential commit hash.

## Hardened migration checkpoint / project review — 2026-09-21

Reviewed continuation against 3cf00e728e168b43652f8f5a1dac595907258bb7, including new untracked source/tests before staging. Owner authorized continuation of local release preparation; earlier all-branch integration is already on remote master at that revision. This checkpoint has no additional push, publication, cloud apply or spending. Canonical task remains Approved / In progress.

AC-03/04 local evidence: additive `migration-hardened` target uses the pinned Node24.21.0/base digest and installed Prisma7.10.0 dependency closure, unchanged schema/migrations and bundled operator command. Existing full migration stage, pnpm invocation, package graph, all prior tests and local serving behavior remain. Register UID1000 in the candidate while retaining all base accounts, compatible with existing job definitions. Native TLS settings are corrected for Prisma's connector without changing pg verification or adding a trust-all fallback. Original migrations, advisory lock, secret separation, fail-closed role review, grants and generic safe failure logging remain.

Verification on macOS ARM64/Docker, Node24.21.0, pnpm10.34.5:

- `pnpm check`: PASS (lint/boundaries/types, 34 unit/API tests, Prisma generation and API/web builds). First sandboxed run could not bind localhost; same tests passed with local binding authorized, no assertions skipped or weakened.
- Final `pnpm lint`, `pnpm typecheck`, `pnpm codegen` and generated-contract diff: PASS/no drift. One shell invocation selected the host pnpm after codegen and hit registry access; rerun used the pinned local runtime, without disabling package verification.
- Final image build `docker build --target migration-hardened --build-arg SOURCE_REVISION=3cf00e7-migration-candidate -f infrastructure/docker/Dockerfile.serving -t mcclinic-migration:focused-task028 .`: PASS.
- `pnpm exec vitest run tests/staging-migration-image.integration.test.ts tests/staging-roles.integration.test.ts tests/staging-tls.integration.test.ts`: PASS, three files/three comprehensive scenarios. New real-image test runs owner bootstrap and migrator deploy twice, validates every original migration checksum/timestamp, uses special-character credentials, verifies one actual runtime update plus audit insert, denies record deletion/history update/persistent DDL, and proves native CLI wrong-CA and wrong-hostname rejection. Existing role/audit rollback and pg TLS tests retained and passed. Non-root UID1000/no shell/npm/pnpm/perl and read-only root with non-executable /tmp checks pass.
- Exact final image ID `sha256:eb16dc5d634ea17eeabcc4987a5974793a43b6b8e5082abf7cc69a975ebb122e`, ARM64. Trivy0.74.0 image scan: 16 low/15 medium, zero high/critical. Separate Prisma131-component and command15-component inventories: no reported vulnerabilities. Full reports, DB timestamps, native-engine hash, build/source manifests and logs: [migration evidence](TASK-028-artifacts/migration-2026-09-21/). Scan warnings preserved. Native embedded dependency/reachability coverage is not certified; no suppressions or risk acceptance.

Investigation preserved in ignored local logs: initial broad candidate carried frontend advisories, so the new candidate was bounded to the frozen CLI closure without changing/removing the original image. Dependency manifests required filesystem resolution for ESM-only exports. Docker test network aliases needed the positive certificate hostname explicitly; a container HOME override mistakenly altered the host Docker context and was corrected before final tests. Distroless UID1000 required a passwd entry for Prisma's native loader. Actual native CLI execution then passed strict TLS tests. No production diagnostic logging or credentials were exposed; tracked evidence contains synthetic-free build/test summaries and public package metadata, not generated database secrets.

Project-review decision: accept this local migration checkpoint. Scope/justification align with REQ-FOUND-012 and ADR-015; no application feature or domain boundary change, no deleted files/migrations/tests/API behavior, no weakened authorization/audit/clinical rules. All original tests are KEEP; the new real-image scenario is ADD. The URL change preserves verified TLS intent with connector-specific options. No additional browser journeys were run for this operator-only follow-up; the preceding unchanged serving/browser evidence is historical, not a new cloud test. Hosted GitHub CI is unverified because gh is unauthenticated.

Unrelated compose.yaml and read-local-mail.ts diffs remain unchanged/excluded (SHA256 c717836a243d8d6c76afcf557cbea8d231473ac67c67b50ab32b4581e76791d6). Remaining next step: bounded local cost/anomaly and operator monitoring preparation, followed by a reviewable account/domain/budget approval package. Original full-image and lower-severity findings remain Open under FIND-013; refresh all image scans before publication/exposure. Actual AWS/RDS, alert delivery, recovery, target-ISP and production policy criteria remain unrun/open. No deployment, clinical/legal readiness or full TASK-028 completion claimed.
